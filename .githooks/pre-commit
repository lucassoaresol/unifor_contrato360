#!/usr/bin/env node

import { spawnSync } from 'node:child_process';

const result = spawnSync('npm', ['run', 'precommit'], { stdio: 'inherit' });
if (result.error) console.error('pre-commit: não foi possível iniciar o guardrail local');
process.exit(result.status ?? 1);
